printf("///////////////\n");
        for ( iter = b.begin(); iter != b.end(); iter ++ )
        {
           cout << iter->first << ':' << iter->second << endl;  
        }
        printf("///////////////\n");