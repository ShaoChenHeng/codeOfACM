#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 10;
int n, m, q;
int a[N], sz[N];

int main()
{
    scanf("%d %d %d",&n, &m, &q);
    for ( int i = 1; i <= n; i++ ) scanf("%d",&a[i]);
    for ( int i = 1; i <= n; i++ ) scanf("%d",&sz[i]);
    return 0;
}
